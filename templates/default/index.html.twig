{% extends 'base.html.twig' %}

{% block title %}Hello AdministrationController!{% endblock %}

{% block body %}
<h1 align="center">Liste des articles</h1>
    <div class="row">
    {% for article in listeArticle %}
    <div class="col-sm-6">
        <div class="card mb-3" style="max-width: 540px;">
        <div class="row g-0">
            <div class="col-md-4">
                <img src="..." class="img-fluid rounded-start" alt="...">
            </div>
            <div class="col-md-8">
                <div class="card-body">
                    <h5 class="card-title"> {{ article.titreArticle }}</h5>
                    <p class="card-text">{{ article.resumeArticle }}</p>
                    <p class="card-text"><button type="button" class="btn btn-info"><a href="#" class="a-link">Accèder a l'article</a></button></p>
                </div>
            </div>
        </div>
        </div> 
    </div>       
    {% endfor %}
    </div>
{% endblock %}
